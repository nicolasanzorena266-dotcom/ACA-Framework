[project]
name = "aca-framework"
version = "0.4.0-sprint72b-rc4"
description = "ACA deterministic cognitive runtime"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["kernel", "."]

